import React from "react";
import ReactDOM from "react-dom";

var numbers = [3, 56, 2, 48, 5];

//Filter - Create a new array by keeping the items that return true.

// 1 Using map done the filter

// const newNumbers = numbers.map(function(num){
//       return num >10
// })

// console.log(newNumbers);

// 2 using foreach we done the filter function
//it's took lot more effect than map filter

// const newNumbers = [];

// numbers.forEach(function (num) {
//   if (num > 10) {
//     newNumbers.push(num);
//   }
// });

// console.log(numNumbers);

// ReactDOM.render(<h1>{newNumbers}</h1>,document.getElementById('root'));
